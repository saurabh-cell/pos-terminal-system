# Cloud Database (Supabase)
spring.datasource.url=jdbc:postgresql://db.tejszkselhueghalpqtc.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=postgres
# Use the password you created when setting up the Supabase project
spring.datasource.password=Baniya@52budhi

# Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# This line forces Hibernate to initialize the database on startup
spring.sql.init.mode=always

# 3. Application Name
spring.application.name=terminal


# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=saurabh52bansal@gmail.com
# Paste the 16-character code here (no spaces)
spring.mail.password=pjig vknj zajo llyb

# Mail Protocol Settings
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=15000
spring.mail.properties.mail.smtp.timeout=15000
spring.mail.properties.mail.smtp.writetimeout=15000